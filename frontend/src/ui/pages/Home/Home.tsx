import FormTests from '../../components/FormTests/FormTests';

const Home = () => {
  return (
    <>
      {/* TODO: remove <FormTests /> when first form is ready  */}
      <FormTests />
    </>
  );
};

export default Home;
